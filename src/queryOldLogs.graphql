query getOldLogs($sweepId: Int, $upTo: Int) {
    run_log(
        where: { run: { sweepid: { _eq: $sweepId } }, id: { _lt: $upTo } }
        order_by: { id: asc }
    ) {
        id
        log
        runid
    }
}
