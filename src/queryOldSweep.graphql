query queryOldSweeps($upTo: Int) {
    sweep(
        where: { id: { _lt: $upTo } }
    ) {
        id
        metadata
        sweep_parameters {
            Key
            Values
        }
    }
}
